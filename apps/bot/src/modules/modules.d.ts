/**
 * Módulo virtual generado por el plugin `module-discovery` de esbuild.
 * Importa automáticamente todos los archivos `{nombre}.module.ts` (o `index.ts`)
 * que existan en src/modules/*
 */
declare module "~modules" {}
